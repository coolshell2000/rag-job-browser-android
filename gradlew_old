#!/bin/sh

#
# Copyright (c) 2015-present, the original author or authors.
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

##############################################################################
#
# Gradle start up script for POSIX generated by Gradle.
#
# Important for running:
#
#   - You need a POSIX-compliant shell to run this script. If your /bin/sh is
#     noncompliant, but you have some other compliant shell such as ksh or
#     bash, then to run this script, type that shell name before the whole
#     command line, like:
#
#       ksh Gradle
#       bash /path/to/Gradle
#
#   - Locate a JDK installation directory as follows:
#
#       If JAVA_HOME is defined, use it.
#       If JAVA_HOME is not defined, look for java in PATH.
#       If JAVA_HOME is not defined and java is not in PATH, look at common
#           installation locations for common distributions of Java.
#
#   - If GRADLE_HOME is not defined, look for a gradle installation under the
#     same directory as this script, or if that does not exist, look under a
#     common /gradle directory.
#
#   - Run a Gradle build using the java command found above, and the Gradle
#     installation found above.
#
#   - To allow running Gradle from any directory, the Gradle start up script is
#     installed in a special way. This script is included with the Gradle
#     installation. You do not need to copy it to another place.
#
#     The script will detect the Gradle installation directory from the location
#     of the script, relative to the running process.
#
#     If you have problems running the script, please contact the Gradle team
#     at https://github.com/gradle/gradle/issues
#
##############################################################################

# Add default JVM options here. You can also use JAVA_OPTS and GRADLE_OPTS to pass JVM options to this script.
DEFAULT_JVM_OPTS='"-Xmx64m" "-Xms64m"'

# Use the maximum available, or set MAX_FD != -1 to use that value.
MAX_FD="maximum"

warn () {
    echo "$*"
} >&2

die () {
    echo
    echo "$*"
    echo
    exit 1
} >&2

# OS specific support (must be 'true' or 'false').
cygwin=false
msys=false
darwin=false
nonstop=false
case "$(uname)" in                #(
  CYGWIN* )           cygwin=true  ;; #(
  Darwin* )           darwin=true  ;; #(
  MSYS* | MINGW* )    msys=true    ;; #(
  NONSTOP* )          nonstop=true ;;
esac

# For Cygwin, ensure paths are in Unix format before anything is touched.
if "$cygwin" ; then
    [ -n "$JAVA_HOME" ] && JAVA_HOME=$(cygpath --unix "$JAVA_HOME")
    [ -n "$CLASSPATH" ] && CLASSPATH=$(cygpath --path --unix "$CLASSPATH")
fi

# Check if the script location is defined properly
if [ -z "$APP_HOME" ] ; then
    # Get the absolute path of the script
    case $0 in
        /\*) APP_HOME="$0" ;;
        *) APP_HOME="$(pwd)/$0" ;;
    esac

    # Follow symlinks to get to the real script location
    while
        [ -h "$APP_HOME" ]
    do
        ls=$(ls -ld "$APP_HOME")
        link=$(expr "$ls" : '.*-> \(.*\)
)
        case $link in
            /*) APP_HOME="$link" ;;
            *) APP_HOME="$(dirname "$APP_HOME")/$link" ;;
        esac
    done

    # Change from the script location to the app root location
    APP_HOME="$(dirname "$APP_HOME")"
    # Set APP_BASE_NAME to the name of the script, without the extension
    APP_BASE_NAME="$(basename "$0")"
    # Strip the extension if there is one
    APP_BASE_NAME="${APP_BASE_NAME%%.*}"
fi

# Change current directory to the app location
cd "$APP_HOME" || die "Could not change directory to $APP_HOME"

# Set up the classpath to include the gradle wrapper jar
CLASSPATH="$APP_HOME/gradle/wrapper/gradle-wrapper.jar"

# Determine the Java command to use to start the JVM.
if [ -n "$JAVA_HOME" ] ; then
    if [ -x "$JAVA_HOME/jre/sh/java" ] ; then
        # IBM's JDK on AIX uses strange locations for the executables
        JAVACMD="$JAVA_HOME/jre/sh/java"
    else
        JAVACMD="$JAVA_HOME/bin/java"
    fi
    if [ ! -x "$JAVACMD" ] ; then
        die "ERROR: JAVA_HOME is set to an invalid directory: $JAVA_HOME

Please set the JAVA_HOME variable in your environment to match the
location of your Java installation."
    fi
else
    JAVACMD="java"
    which java >/dev/null 2>&1 || die "ERROR: JAVA_HOME is not set and no 'java' command could be found in your PATH.

Please set the JAVA_HOME variable in your environment to match the
location of your Java installation."
fi

# Increase the maximum file descriptors if we can.
if ! "$cygwin" && ! "$darwin" && ! "$nonstop" ; then
    case $MAX_FD in #(
      max*)
        # In POSIX sh, ulimit -H is undefined. That's why the result is checked and
        # the value is set to a reasonable default if ulimit fails.
        # shellcheck disable=SC2039
        MAX_FD=$(ulimit -H -n) ||
            warn "Could not query maximum file descriptor limit: ulimit -H -n" ||
            MAX_FD=4096
        # shellcheck disable=SC2039
        ulimit -n $MAX_FD ||
            warn "Could not set maximum file descriptor limit: ulimit -n $MAX_FD" ;;
    esac
fi

# Collect all arguments for the java command:
#   * DEFAULT_JVM_OPTS
#   * JAVA_OPTS
#   * GRADLE_OPTS
#   * -D...appname settings
#   * --module-path (only if needed)
#   * -classpath
#   * the main class name
#   * args from the command line

# For Cygwin or MSYS, switch paths to Windows format before running java
if "$cygwin" || "$msys" ; then
    APP_HOME=$(cygpath --path --mixed "$APP_HOME")
    CLASSPATH=$(cygpath --path --mixed "$CLASSPATH")

    JAVACMD=$(cygpath --unix "$JAVACMD")
    # shellcheck disable=SC2039
    [ -n "$JAVA_HOME" ] && JAVA_HOME=$(cygpath --path --mixed "$JAVA_HOME")
fi

# Use "x" prefix to make it work with option arguments that begin with a dash
# shellcheck disable=SC2039
exec "$JAVACMD" \
    $DEFAULT_JVM_OPTS \
    $JAVA_OPTS \
    $GRADLE_OPTS \
    -Dorg.gradle.appname="$APP_BASE_NAME" \
    -classpath "$CLASSPATH" \
    org.gradle.wrapper.GradleWrapperMain \
    "$@"
